# Cloudflare Pages — 台指交易看板（靜態 HTML + /api/* Functions）
# Deploy command 無法留空時，用：npm run deploy（勿用 npx wrangler deploy）
name = "trading-strstegy"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."
