# Cloudflare Pages — 台指交易看板（靜態 HTML + /api/* Functions）
# 請用「Pages」連 GitHub，不要用 Workers 的 npx wrangler deploy
name = "taiwan-trading-dashboard"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."
